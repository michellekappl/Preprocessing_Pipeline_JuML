"""
lemmatize(input) lemmatizes regular words, based on some basic rules.
only for english language
"""

function lemmatize(input::Vector{String})
    #no idea how to do it with irregular words and so on,
    #still a to do in general
end
export lemmatize

